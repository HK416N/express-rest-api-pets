info:
  name: all-pets
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}"
  params:
    - name: maxRecords
      value: "3"
      type: query
      disabled: true
    - name: view
      value: Grid%20view
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
