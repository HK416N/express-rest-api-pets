info:
  name: create-pet
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}"
  body:
    type: json
    data: |-
      {
        "fields": {
          "Name": "dog",
          "Breed": "boxer",
          "age": 3
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
