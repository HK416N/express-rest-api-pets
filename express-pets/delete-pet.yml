info:
  name: delete-pet
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/:petId"
  params:
    - name: petId
      value: recqK1d9u4GS9rgdh
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
