opencollection: 1.0.0

info:
  name: seb-61-local-pets
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  headers:
    - name: Authorization
      value: Bearer {{process.env.VITE_AIRTABLE_TOKEN}}
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
